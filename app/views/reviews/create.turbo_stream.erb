<% if @product.reviews.any? %>
  <%= turbo_stream.update "list_reviews_product_#{@product.id}", partial: "reviews/review", locals: {reviews: @product.reviews } %>
<% end %>
<%= turbo_stream.update "statistic_star", partial: "products/statistic", locals: { product: @product } %>

<%= turbo_stream.update "reviewer_number" do %>
  <span class="text-gray-600 ml-3"><%= @product.reviews.count(:user_id) %> Reviews</span>
<% end %>
<%= turbo_stream.update "list_reviews_product_#{@product.id}", partial: "reviews/review", locals: { reviews: @product.reviews }%>