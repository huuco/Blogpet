<!DOCTYPE html>
<html>
  <head>
    <title>Blogpet</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-white <%= request.path == "/blogs" ? 'relative w-full h-full bg-white md:bg-violet-50 antialiased text-gray-600' : ''%>">
    <%= render "layouts/header" %>
    <main class="<%= request.path == "/blogs" ? 'flex-1 pt-20' : 'container px-4 z-0' %>" role="<%= request.path == '/blogs' ? 'main' : '' %>">
      <%= yield %>
    </main>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= render "carts/cart", product_carts: @product_carts %>
    <%= render "shared/login" %>
  </body>
</html>
