<% product_carts&.each do |product, count| %>
  <%= turbo_frame_tag "product_item_#{product.id}" do %>
    <li class="flex py-6" data-controller="product" data-price="<%= product.price * count %>">
      <div class="h-24 w-24 flex-shrink-0 overflow-hidden rounded-md border border-gray-200">
        <img src="https://tailwindui.com/img/ecommerce-images/shopping-cart-page-04-product-02.jpg" alt="Front of satchel with blue canvas body, black straps and handle, drawstring top, and front zipper pouch." class="h-full w-full object-cover object-center">
      </div>

      <div class="ml-4 flex flex-1 flex-col">
        <div>
          <div class="flex justify-between text-base font-medium text-gray-900">
            <h3>
              <%= link_to product.name, product %>
            </h3>
            <p class="ml-4">$<%= product.price %></p>
          </div>
          <p class="mt-1 text-sm text-gray-500">Blue</p>
        </div>
        <div class="flex flex-1 items-end justify-between text-sm">
          <p class="text-gray-500">Qty <%= count %></p>

          <div class="flex">
            <%= link_to "Remove", remove_to_cart_path(product_id: product.id), data: {turbo_method: :delete, action: "click->product#remove"}, class: "font-medium text-indigo-600 hover:text-indigo-500"%>
          </div>
        </div>
      </div>
    </li>
  <%end%>
<%end%>

