<div class="comments-widget" id="comments-widget-of-commentable-<%= commentable.id %>">
  <h2 class="comments-header">
    Comments
    <small id="comments-count-of-commentable-<%= commentable.id %>"><%= commentable.comments.count %></small>
  </h2>
  <ul class="comments-list">
    <div class="comment-form">
      <%= render "comments/form", comment: commentable.comments.build %>
    </div>
    <%= render commentable.comments.where(parent: nil).order(created_at: :desc) %>
  </ul>
</div>